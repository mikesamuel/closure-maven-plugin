syntax = "proto2";

package com.example;

import "webutil/html/types/html.proto";

/* A user-name is either a string of text or of HTML */
message UserName {
  optional string text = 1;
  optional SafeHtmlProto html = 2;
}
