<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.google.common.html.it</groupId>
  <artifactId>simple-it</artifactId>
  <version>1.0-SNAPSHOT</version>

  <description>Integration test</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <build>
    <defaultGoal>test</defaultGoal>
    <plugins>
      <plugin>
        <groupId>com.google.common.html</groupId>
        <artifactId>closure-maven-plugin</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <executions>
          <execution>
            <phase>generate-sources</phase>
            <goals>
              <goal>generate-sources</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <extracts>
            <extract>proto</extract>
          </extracts>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <!-- The protoc plugin needs protobuf-java so it can figure out which
         version of protoc to invoke. -->
    <dependency>
      <groupId>com.google.protobuf</groupId>
      <artifactId>protobuf-java</artifactId>
      <version>3.0.0-beta-2</version>
    </dependency>
    <!-- Above we specify that we're going to extract supporting files from this.
         Specifically, html.proto -->
    <dependency>
      <groupId>com.google.common.html.types</groupId>
      <artifactId>proto</artifactId>
      <version>1.0.4-SNAPSHOT</version>
    </dependency>
    <!-- Our JUnit tests run JBC templates -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.8.2</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.google.template</groupId>
      <artifactId>soy</artifactId>
      <version>2016-07-23-SNAPSHOT</version>
    </dependency>
    <!-- Our tests depend upon the compiled soy templates -->
    <!--dependency>
      <groupId>greetings.world</groupId>
      <artifactId>soy-compiled</artifactId>
      <version>1.0</version>
      <scope>system</scope>
      <systemPath>${project.build.directory}/closure-templates-soy.0.jar</systemPath>
    </dependency-->
  </dependencies>
</project>
